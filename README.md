# clt_app

This repo includes the code for https://thodk.shinyapps.io/clt_app/ application, an implementation of Central Limit Theorem.

ui.R script contains the user interface structure, embedded input forms and the appropriate documentation & instructions.
server.R script is the "backend" operator, which performs basic calculations and send to browser interface the result/plot.
